# Spec-Kit 指令快速參考指南

## 📚 指令總覽

| 指令 | 用途一句話 | 使用時機 |
|------|----------|---------|
| `/speckit.constitution` | 定義專案的核心原則和開發規範 | 專案開始時（可選） |
| `/speckit.specify` | 將功能需求轉化為清晰的規格文件 | 開發新功能的第一步 |
| `/speckit.clarify` | 解決規格中的模糊和歧義問題 | 規格化後、規劃前（可選） |
| `/speckit.plan` | 制定功能的技術實作方案 | 規格明確後 |
| `/speckit.tasks` | 將技術方案分解為可執行的任務清單 | 技術方案完成後 |
| `/speckit.analyze` | 檢查規格、計畫、任務的一致性 | 任務分解後、實作前（可選） |
| `/speckit.checklist` | 產生需求品質驗證清單 | 任何階段驗證品質 |
| `/speckit.implement` | 按任務清單逐步實作功能程式碼 | 所有準備工作完成後 |

---

## 🎯 核心工作流程（必要）

### 1️⃣ `/speckit.specify` - 規格化
**用途**：將功能需求轉化為清晰的規格文件

**何時使用**：開發新功能的第一步

**輸入範例**：
```
/speckit.specify

開發一個使用者註冊功能。使用者可以透過信箱和密碼註冊帳號，
系統需要驗證信箱格式，密碼強度至少8位包含字母和數字。
```

**輸出**：
- `spec.md` - 功能規格文件
- `checklists/requirements.md` - 品質檢查清單

---

### 2️⃣ `/speckit.plan` - 技術規劃
**用途**：制定功能的技術實作方案

**何時使用**：規格明確後

**輸入範例**：
```
/speckit.plan
```

**輸出**：
- `plan.md` - 技術實作計畫
- `data-model.md` - 資料模型
- `contracts/` - API 介面定義
- `research.md` - 技術決策

---

### 3️⃣ `/speckit.tasks` - 任務分解
**用途**：將技術方案分解為可執行的任務清單

**何時使用**：技術方案完成後

**輸入範例**：
```
/speckit.tasks
```

**輸出**：
- `tasks.md` - 詳細任務清單

---

### 4️⃣ `/speckit.implement` - 實作
**用途**：按任務清單逐步實作功能程式碼

**何時使用**：所有準備工作完成後

**輸入範例**：
```
/speckit.implement
```

**效果**：
- 按階段執行任務
- 遵循 TDD 原則
- 自動追蹤進度

---

## 🔧 輔助指令（可選）

### 🅰️ `/speckit.constitution` - 專案原則
**用途**：定義專案的核心原則和開發規範

**何時使用**：專案開始時（可選）

**輸入範例**：
```
/speckit.constitution

我們的專案遵循：
1. 測試驅動開發（TDD）是強制的
2. 程式碼必須有詳細的文件註釋
3. 使用 TypeScript + React
```

**輸出**：
- `.specify/memory/constitution.md` - 專案章程

---

### 🅱️ `/speckit.clarify` - 需求澄清
**用途**：解決規格中的模糊和歧義問題

**何時使用**：規格化後、規劃前（可選）

**輸入範例**：
```
/speckit.clarify
```

**效果**：
- 識別需求中的不明確之處
- 提出結構化問題
- 更新規格文件

---

### 🅲 `/speckit.analyze` - 一致性分析
**用途**：檢查規格、計畫、任務的一致性

**何時使用**：任務分解後、實作前（可選）

**輸入範例**：
```
/speckit.analyze
```

**輸出**：
- 一致性分析報告
- 問題清單和修復建議

---

### 🅳 `/speckit.checklist` - 品質檢查
**用途**：產生需求品質驗證清單

**何時使用**：任何階段驗證品質

**輸入範例**：
```
/speckit.checklist

為登入功能建立安全檢查清單
```

**輸出**：
- `checklists/[名稱].md` - 客製化檢查清單

---

## 🚀 完整流程範例

```bash
# 步驟 0（可選）：定義專案原則
/speckit.constitution
我們的專案遵循 TDD、RESTful API 設計

# 步驟 1：建立功能規格 ✅ 必要
/speckit.specify
開發待辦事項管理功能...

# 步驟 2（可選）：澄清需求
/speckit.clarify

# 步驟 3：制定技術方案 ✅ 必要
/speckit.plan

# 步驟 4（可選）：產生檢查清單
/speckit.checklist
建立 UX 檢查清單

# 步驟 5：分解任務 ✅ 必要
/speckit.tasks

# 步驟 6（可選）：一致性檢查
/speckit.analyze

# 步驟 7：開始實作 ✅ 必要
/speckit.implement
```

---

## 💡 使用技巧

### 1. 最小工作流程（4 步）
如果時間緊張，只需執行核心 4 步：
1. `/speckit.specify` - 規格化
2. `/speckit.plan` - 技術規劃
3. `/speckit.tasks` - 任務分解
4. `/speckit.implement` - 實作

### 2. 品質優先工作流程（7 步）
如果追求高品質，建議完整流程：
1. `/speckit.constitution` - 定義原則（可選）
2. `/speckit.specify` - 規格化
3. `/speckit.clarify` - 澄清需求（可選）
4. `/speckit.plan` - 技術規劃
5. `/speckit.tasks` - 任務分解
6. `/speckit.analyze` - 一致性檢查（可選）
7. `/speckit.implement` - 實作

### 3. 靈活使用
- **檢查清單** (`/speckit.checklist`) 可以在任何階段使用
- **澄清** (`/speckit.clarify`) 可以多次運行
- **分析** (`/speckit.analyze`) 建議在實作前運行

---

## 📊 指令優先級

### 🔴 必要（核心流程）
1. `/speckit.specify` - 規格化
2. `/speckit.plan` - 技術規劃
3. `/speckit.tasks` - 任務分解
4. `/speckit.implement` - 實作

### 🟡 推薦（提升品質）
5. `/speckit.clarify` - 需求澄清
6. `/speckit.analyze` - 一致性分析

### 🟢 可選（按需使用）
7. `/speckit.constitution` - 專案原則
8. `/speckit.checklist` - 品質檢查

---

## 🎯 何時使用每個指令？

| 場景 | 推薦指令 |
|------|---------|
| 開始新專案 | `/speckit.constitution` |
| 開發新功能 | `/speckit.specify` → `/speckit.plan` → `/speckit.tasks` → `/speckit.implement` |
| 需求不清楚 | `/speckit.clarify` |
| 擔心品質 | `/speckit.analyze` + `/speckit.checklist` |
| 快速原型 | `/speckit.specify` → `/speckit.implement`（跳過中間步驟） |
| 團隊協作 | 完整流程（所有指令） |

---

**快速查找指令用途，高效使用 Spec-Kit！** 🚀
